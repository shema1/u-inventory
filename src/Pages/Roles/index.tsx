import { Table } from "antd"
import MainLayout from "../../Layout/MainLayout"
import { FC } from "react";



const Roles: FC = () => {
    return <>
        {/* <MainLayout>
            <Table<IRole> columns={columns} dataSource={roles} loading={isLoading} />
        </MainLayout> */}
    </>
}

export default Roles;